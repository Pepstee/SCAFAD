"""Backend-local tests for the SCAFAD GUI.

The authoritative test suite lives under ``tests/unit/test_gui_backend_*.py``
and ``tests/integration/test_gui_*``.  This sub-package only houses fixtures
shared between those tests via ``pytest`` conftest discovery if needed.
"""
